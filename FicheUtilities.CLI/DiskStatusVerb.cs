namespace FicheUtilities.CLI
{
    //[Verb("disk", HelpText = "Get disk usage status")]
    //class DiskStatusVerb
    //{
    //    public DiskStatusVerb(string path, double warning, double critical, bool verbose = false)
    //    {
    //        Diskpath = path;
    //        Warning = warning;
    //        Critical = critical;
    //        Verbose = verbose;
    //    }

    //    [Option('p', "path", Required = true, HelpText = "Path to the disk to be checked. This can either be a UNC path or a drive letter.")]
    //    public string Diskpath { get; }

    //    [Option('w', "warning", Required = true, HelpText = "Threshold, in gigabytes, at which a warning result should be returned.")]
    //    public double Warning { get; }

    //    [Option('c', "critical", Required = true, HelpText = "Threshold, in gigabytes, at which a critical result should be returned.")]
    //    public double Critical { get; }

    //    [Option('v', "verbose", Required = false, HelpText = "Display full disk statistics.")]
    //    public bool Verbose { get; }
    //}
}
